# Workspace artifacts (visible when checking out this orphan branch
# while the main branch working tree is on disk)
/target/
Cargo.lock
*.swp
*.swo
schemas/
core/
cli/
db/
discovery/
sqlite/
examples/
docs/
src/
